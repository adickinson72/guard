---
apiVersion: v1
kind: ConfigMap
metadata:
  name: guard-config
  namespace: guard-system
  labels:
    app.kubernetes.io/name: guard
    app.kubernetes.io/component: automation
data:
  # Add your GUARD configuration here
  # Example:
  # config.yaml: |
  #   clusters:
  #     - cluster_id: "prod-us-east-1"
  #       environment: "production"
  #       ...
